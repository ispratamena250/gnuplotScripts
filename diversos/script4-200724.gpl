#Script para plot da função f(x)=x**2/3 no intervalo [-2,3]

set xrange [-2:3]
set yrange [-1:3]
set xlabel "x"
set ylabel "f(x)"
set title "Observando a função f(x) - máximos e mínimos"

f(x) = (abs(x)**(2/3.0)) * (x < 0 ? -1:1)

plot f(x) with lines lc rgb 'blue' title "f(x)=x**2/3"
